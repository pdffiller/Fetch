verbose_ssl = yes
auth_verbose = yes
auth_debug = yes
auth_debug_passwords = yes
disable_plaintext_auth = no

userdb {
  driver = passwd
  args = blocking=no
  override_fields = home=/home/%u mail=maildir:/home/%u/Maildir
}
